info:
  name: Deck_Create
  type: http
  seq: 1

http:
  method: POST
  url: 0.0.0.0:3000/api/decks
  headers:
    - name: x-dev-user-id
      value: "100.0"
  body:
    type: json
    data: |-
      {
        "name": "Test Deck"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
