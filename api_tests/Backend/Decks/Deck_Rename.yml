info:
  name: Deck_Rename
  type: http
  seq: 1

http:
  method: POST
  url: 0.0.0.0:3000/api/decks/:id/rename
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Test Deck Renamed"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
