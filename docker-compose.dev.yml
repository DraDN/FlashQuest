services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"

  backend:
    command: npm run dev
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development